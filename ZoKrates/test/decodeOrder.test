import "./../decodeOrder.code"

def main():
  amount, sourceToken, targetToken, limit = decodeOrder(0)
  amount == 0
  sourceToken == 0
  targetToken == 0
  limit == 0

  amount, sourceToken, targetToken, limit = decodeOrder(13921004799855423936921571342316048744460)
  amount == 12
  sourceToken == 9
  targetToken == 7
  limit == 10
  return 0